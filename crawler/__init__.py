"""Deterministic crawling layer: HTTP retrieval, URL handling, page parsing.

No AI logic lives here. Modules are organization-type agnostic so future
target types (blogs, labels, curators, ...) reuse them unchanged.
"""
